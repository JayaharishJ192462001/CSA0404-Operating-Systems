#include <stdio.h>

int main() {
    FILE *fp;
    char msg[20];

    fp = fopen("ipc.txt", "w");
    fprintf(fp, "Hello Child");
    fclose(fp);

    fp = fopen("ipc.txt", "r");
    fscanf(fp, "%s", msg);
    printf("Message received: %s", msg);
    fclose(fp);

    return 0;
}

